# Configuración de UID/GID para evitar problemas de permisos
# Estos valores deben coincidir con el UID/GID del usuario del host
# 
# Para obtener tu UID/GID en Linux/WSL, ejecuta:
#   id -u  (para UID)
#   id -g  (para GID)
#
# Valores predeterminados: 1000 (común en la mayoría de sistemas Linux)

UID=1000
GID=1000
